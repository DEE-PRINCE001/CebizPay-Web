Now, let's wire it up with the right api for each of the data in the page. check all the available api endpoint(you can check in
  the file or even validate using the live swagger endpoint)  and integrate each of the data in the page to the right endpoint.
  before integrating any api, test it live and investigate the type of different response they can send and the right payload, so
  as to wire them up correctly. let me know if any credentials will be needed to carry this out. Also, the backend does not have
  much data now, most of the data may come empty, so for testing purpose provide a fallback data for the ui when the data is empty
  and this must be able to be deleted easily for production later.

  Also, let me know if there is any endpoint that would be needed from the backend that is not yet available with the type of data
  you need from it.